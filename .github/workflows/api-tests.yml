name: API End-to-End Tests

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  test-api:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        include:
          - endpoint: "/"
            name: "All localities"
          - endpoint: "/judete/"
            name: "List counties"
          - endpoint: "/ROMANIA/"
            name: "List all counties"
          - endpoint: "/ROMANIA/ALBA/"
            name: "ALBA county"
          - endpoint: "/ROMANIA/ARAD/"
            name: "ARAD county"
          - endpoint: "/ROMANIA/ARGES/"
            name: "ARGES county"
          - endpoint: "/ROMANIA/BACAU/"
            name: "BACAU county"
          - endpoint: "/ROMANIA/BIHOR/"
            name: "BIHOR county"
          - endpoint: "/ROMANIA/BISTRITA-NASAUD/"
            name: "BISTRITA-NASAUD county"
          - endpoint: "/ROMANIA/BOTOSANI/"
            name: "BOTOSANI county"
          - endpoint: "/ROMANIA/BRAILA/"
            name: "BRAILA county"
          - endpoint: "/ROMANIA/BRASOV/"
            name: "BRASOV county"
          - endpoint: "/ROMANIA/BUZAU/"
            name: "BUZAU county"
          - endpoint: "/ROMANIA/CALARASI/"
            name: "CALARASI county"
          - endpoint: "/ROMANIA/CARAS-SEVERIN/"
            name: "CARAS-SEVERIN county"
          - endpoint: "/ROMANIA/CLUJ/"
            name: "CLUJ county"
          - endpoint: "/ROMANIA/CONSTANTA/"
            name: "CONSTANTA county"
          - endpoint: "/ROMANIA/COVASNA/"
            name: "COVASNA county"
          - endpoint: "/ROMANIA/DAMBOVITA/"
            name: "DAMBOVITA county"
          - endpoint: "/ROMANIA/DOLJ/"
            name: "DOLJ county"
          - endpoint: "/ROMANIA/GALATI/"
            name: "GALATI county"
          - endpoint: "/ROMANIA/GIURGIU/"
            name: "GIURGIU county"
          - endpoint: "/ROMANIA/GORJ/"
            name: "GORJ county"
          - endpoint: "/ROMANIA/HARGHITA/"
            name: "HARGHITA county"
          - endpoint: "/ROMANIA/HUNEDOARA/"
            name: "HUNEDOARA county"
          - endpoint: "/ROMANIA/IALOMITA/"
            name: "IALOMITA county"
          - endpoint: "/ROMANIA/IASI/"
            name: "IASI county"
          - endpoint: "/ROMANIA/ILFOV/"
            name: "ILFOV county"
          - endpoint: "/ROMANIA/MARAMURES/"
            name: "MARAMURES county"
          - endpoint: "/ROMANIA/MEHEDINTI/"
            name: "MEHEDINTI county"
          - endpoint: "/ROMANIA/MURES/"
            name: "MURES county"
          - endpoint: "/ROMANIA/NEAMT/"
            name: "NEAMT county"
          - endpoint: "/ROMANIA/OLT/"
            name: "OLT county"
          - endpoint: "/ROMANIA/PRAHOVA/"
            name: "PRAHOVA county"
          - endpoint: "/ROMANIA/SALAJ/"
            name: "SALAJ county"
          - endpoint: "/ROMANIA/SATUMARE/"
            name: "SATUMARE county"
          - endpoint: "/ROMANIA/SIBIU/"
            name: "SIBIU county"
          - endpoint: "/ROMANIA/SUCEAVA/"
            name: "SUCEAVA county"
          - endpoint: "/ROMANIA/TELEORMAN/"
            name: "TELEORMAN county"
          - endpoint: "/ROMANIA/TIMIS/"
            name: "TIMIS county"
          - endpoint: "/ROMANIA/TULCEA/"
            name: "TULCEA county"
          - endpoint: "/ROMANIA/VALCEA/"
            name: "VALCEA county"
          - endpoint: "/ROMANIA/VASLUI/"
            name: "VASLUI county"
          - endpoint: "/ROMANIA/VRANCEA/"
            name: "VRANCEA county"
          - endpoint: "/ROMANIA/BUCURESTI/"
            name: "BUCURESTI county"

    steps:
      - name: Test endpoint ${{ matrix.endpoint }}
        run: |
          URL="https://orase.peviitor.ro${{ matrix.endpoint }}"
          echo "Testing: $URL"
          
          # Get response
          RESPONSE=$(curl -s -w "\n%{http_code}" "$URL")
          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')
          
          # Check HTTP status
          if [ "$HTTP_CODE" != "200" ]; then
            echo "❌ FAILED: HTTP $HTTP_CODE"
            echo "Response: $BODY"
            exit 1
          fi
          echo "✓ HTTP 200 OK"
          
          # Check for PHP errors
          if echo "$BODY" | grep -qi "php error\|parse error\|fatal error\|warning"; then
            echo "❌ FAILED: PHP error detected"
            echo "$BODY"
            exit 1
          fi
          echo "✓ No PHP errors"
          
          # Validate JSON
          if ! echo "$BODY" | jq . > /dev/null 2>&1; then
            echo "❌ FAILED: Invalid JSON response"
            echo "$BODY"
            exit 1
          fi
          echo "✓ Valid JSON"
          
          # Check response is not empty
          if [ -z "$BODY" ] || [ "$BODY" = "[]" ] || [ "$BODY" = "{}" ]; then
            echo "❌ FAILED: Empty response"
            exit 1
          fi
          echo "✓ Response not empty"
          
          echo "✅ PASSED: ${{ matrix.name }}"
