name: IASI County Tests (Manual)

on:
  workflow_dispatch:

jobs:
  test-braila:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Install dependencies
        run: php -m

      - name: Run IASI tests
        id: test
        run: |
          mkdir -p testing/IASI/reports
          cd testing/IASI
          php run_tests.php

      - name: List all reports
        run: |
          echo "=== All test reports for IASI ==="
          ls -la testing/IASI/reports/

      - name: Upload IASI test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: braila-test-reports-${{ github.run_number }}
          path: testing/IASI/reports/
          retention-days: 30

      - name: Display latest report
        run: |
          LATEST=$(ls -td testing/IASI/reports/*/ | head -1)
          echo "=== Latest Report: $LATEST ==="
          cat "$LATEST/report.html"
